services:
  - type: web
    name: abriblue-traduction-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --limit-request-line 0 --limit-request-field-size 0 serveur_traduction:app
